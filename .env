# Local secrets — gitignored, never committed.

# Powers the grounded chatbot answer layer (/ask endpoint).
# Replace the line below with your real key: OPENAI_API_KEY=sk-proj-xxxxxxxx
OPENAI_API_KEY=sk-proj-REPLACE_WITH_YOUR_OPENAI_KEY

# Gemini free-tier backend for /ask + /diagnose. Set provider to gemini.
LLM_PROVIDER=gemini
GEMINI_API_KEY=AQ.GEMINI_API_KEY
