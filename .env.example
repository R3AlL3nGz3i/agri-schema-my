# Used by the professor-review pipeline (Anthropic).
ANTHROPIC_API_KEY=sk-ant-...

# Used by the grounded chatbot answer layer (/ask endpoint, OpenAI).
# The search itself works without this — it only powers the AI-written answer.
OPENAI_API_KEY=sk-...
